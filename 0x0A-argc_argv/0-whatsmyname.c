#include <stdio.h>
/**
 * main -  writes its name
 * @argc: contains arguement count
 * @argv: contains array of pointers to arguement strings
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
